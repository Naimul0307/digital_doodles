<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Digital Doodles</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="stylesheet" type="text/css" href="{{ url_for('static', filename='css/styles.css') }}">
    <style>
        /* Custom styles for the page */
        body {
            background: url("{{ url_for('static', filename='background image/main.jpg') }}") no-repeat center center fixed;
            background-size: cover;
            height: 100vh; /* Ensure full viewport height */
            margin: 0; /* Remove default margin */
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            overflow: hidden; /* Prevent scrolling */
        }

        .section {
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            width: 100%;
            height: 100%;
        }

        .canvas-container {
            position: relative;
            width: 800px; /* Fixed width for the canvas container */
            height: 800px; /* Fixed height for the canvas container */
            margin: 10px 0; /* Adjust margin as needed */
            animation: fadeInUp 0.5s ease-in-out; /* Example animation for canvas container */
            display: flex;
            justify-content: center;
            align-items: center;
            border: 4px solid #000; /* Simple border around the canvas container */
            box-sizing: border-box; /* Include border in width calculations */
        }

        canvas {
            width: 100%; /* Ensure the canvas fills its container */
            height: 100%; /* Ensure the canvas fills its container */
            display: block;
            border: none; /* Remove default canvas border */
        }

        .toolbar {
            display: flex;
            justify-content: center;
            flex-wrap: wrap;
            gap: 10px;
            background-color: rgba(255, 255, 255, 0.8); /* Semi-transparent white background */
            padding: 10px;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            animation: slideInUp 0.5s ease-in-out; /* Example animation for toolbar */
        }

        /* Button hover animation */
        .btn {
            transition: transform 0.3s ease-in-out;
        }

        .btn:hover {
            transform: scale(1.1);
        }

        /* Floating animation for buttons */
        @keyframes floatAnimation {
            0%, 100% {
                transform: translateY(0);
            }
            50% {
                transform: translateY(-10px);
            }
        }

        .btn-float {
            animation: floatAnimation 3s ease-in-out infinite; /* Adjust duration and animation properties */
        }

        /* Custom animations */
        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes slideInUp {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Custom color for h1 */
        h1 {
            color: #ffffff; /* White color */
        }

        /* Responsive styles */
        @media (max-width: 767px) {
            .canvas-container {
                width: 90%; /* Adjust width for smaller screens */
                height: auto; /* Allow height to adjust based on content */
                margin: 5px 0; /* Reduce margin for smaller screens */
            }
            .toolbar {
                flex-direction: column;
                padding: 5px;
            }
            .btn {
                width: 100%; /* Full width buttons for smaller screens */
                margin-bottom: 5px; /* Add spacing between buttons */
            }
        }

        @media (orientation: landscape) {
            body {
                height: auto; /* Adjust height for landscape mode */
                overflow-y: auto; /* Allow vertical scrolling in landscape */
            }
        }

        /* Tablet specific styles */
        @media (min-width: 768px) and (max-width: 1024px) {
            .canvas-container {
                width: 80%; /* Adjust width for tablets */
                height: auto; /* Allow height to adjust based on content */
                max-width: 700px; /* Max width for tablets */
            }
            .toolbar {
                flex-direction: row;
                flex-wrap: nowrap;
                justify-content: space-around;
            }
            .btn {
                width: auto;
                margin: 5px;
            }
        }
    </style>
</head>
<body>
    <div class="section">
        <div class="container-fluid">
            <div class="row">
                <div class="col-12 text-center">
                    <h1 class="mt-5">Digital Doodles</h1>
                </div>
            </div>
        </div>
    
        <div class="canvas-container">
            <canvas id="doodleCanvas" width="800" height="600"></canvas>
        </div>
    
        <div class="toolbar">
            <button class="btn btn-primary mr-2 btn-float" onclick="clearCanvas()">Clear</button>
            <button class="btn btn-success mr-2 btn-float" onclick="addText()">Add Text</button>
            <button class="btn btn-danger btn-float" onclick="saveDoodle()">Save</button>
        </div>    
    </div>

    <script src="https://cdn.socket.io/4.0.0/socket.io.min.js"></script>
    <script src="{{ url_for('static', filename='js/scripts.js') }}"></script>
</body>
</html>
